---
title: Payment Method Manager 2
---

import { PaymentMethodManager2Demo } from '@/registry/billingsdk/demo/payment-method-manager-2-demo';

The Payment Method Manager 2 component displays saved payment methods and provides actions to add, remove, edit, set a default, or view details.

<Tabs items={["Preview", "Code"]} className="bg-transparent border-none">
  <Tab value="Preview" className="border-none bg-transparent p-0 mt-3">
    <PreviewComponents registryName="billingsdk">
      <PaymentMethodManager2Demo />
    </PreviewComponents>
  </Tab>

  <Tab value="Code" className="mt-3">
    <include
      cwd
      lang="tsx"
      meta='title="src/registry/billingsdk/demo/payment-method-manager-2-demo.tsx"'
    >
      src/registry/billingsdk/demo/payment-method-manager-2-demo.tsx
    </include>
  </Tab>
</Tabs>

## Props

| Prop           | Type                      | Description                                                       |
| -------------- | ------------------------- | ----------------------------------------------------------------- |
| paymentMethods | `PaymentMethod2[]`        | Array of saved payment methods                                    |
| onAdd          | `(() => void)?`           | Function called when add payment method button is clicked         |
| onEdit         | `((id: string) => void)?` | Function called when edit payment method button is clicked        |
| onRemove       | `((id: string) => void)?` | Function called when remove payment method button is clicked      |
| onSetDefault   | `((id: string) => void)?` | Function called when set default button is clicked                |
| onViewDetails  | `((id: string) => void)?` | Function called when view details button is clicked               |
| className      | `string?`                 | Additional CSS classes to apply to the component                  |
| isLoading      | `boolean?`                | Whether the component is in a loading state (defaults to false)   |
| error          | `string \| null?`         | Error message to display if there was an error (defaults to null) |

### PaymentMethod2 Object

| Property       | Type                                  | Description                                    |
| -------------- | ------------------------------------- | ---------------------------------------------- |
| id             | `string`                              | Unique identifier for the payment method       |
| type           | `"credit" \| "ach"`                   | Type of payment method                         |
| last4          | `string`                              | Last 4 digits of the payment method            |
| expiry         | `string?`                             | Expiration date (for cards)                    |
| isDefault      | `boolean`                             | Whether this is the default payment method     |
| brand          | `string?`                             | Brand of the card (e.g., "Visa", "Mastercard") |
| bankName       | `string?`                             | Name of the bank (for ACH accounts)            |
| cardholderName | `string?`                             | Name of the cardholder                         |
| status         | `"active" \| "expired" \| "inactive"` | Status of the payment method                   |
| createdAt      | `string`                              | Date when the payment method was created       |

Note: `expiry` should be "MM/YY"; `createdAt` should be ISO 8601 (e.g., "2025-09-07T12:00:00Z").

<div className="mt-8 pt-8 border-t border-gray-200 dark:border-gray-800">
	<div className="flex items-center justify-between">
		<div>
			<span className="text-sm text-gray-500 dark:text-gray-400">
				Created by{' '}
				<a
					href="https://github.com/sapatmohit"
					className="text-blue-600 hover:underline dark:text-blue-400"
					target="_blank"
					rel="noopener noreferrer"
				>
					Mohit Sapat
				</a>
			</span>
		</div>
	</div>
</div>
